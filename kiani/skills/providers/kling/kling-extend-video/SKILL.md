---
name: kling-extend-video
description: >
  Use this skill for the Kling AI Video Extension API. Extend existing videos by appending new generated content based on text prompts.
---

# Kling Video Extension

Extend existing videos by appending new generated content. Supports videos generated by text-to-video, image-to-video, and previous video extension tasks.

**Provider:** Kling AI
**API Base:** https://api-singapore.klingai.com

---

## API Reference

### Create Task

**POST** `/v1/videos/video-extend`

#### Request Body

| Name | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| `video_id` | `string` | Required |  | Video ID. Supports IDs from text-to-video, image-to-video, and video extension (cannot exceed 3 minutes). Only V1.0, V1.5, V1.6 model videos supported. Videos cleared 30 days after generation. |
| `prompt` | `string` | Optional |  | Text prompt. Max 2500 characters. |
| `negative_prompt` | `string` | Optional |  | Negative text prompt. Max 2500 characters. |
| `cfg_scale` | `float` | Optional | `0.5` | Prompt reference strength. Range: [0, 1]. Higher = stronger reference. |
| `watermark_info` | `object` | Optional |  | Watermark config. `{ "enabled": boolean }` |
| `callback_url` | `string` | Optional |  | Callback URL for task status changes |
| `external_task_id` | `string` | Optional |  | Custom task ID. Must be unique per user. |

#### Response

```json
{
  "code": 0,
  "message": "string",
  "request_id": "string",
  "data": {
    "task_id": "string",
    "task_status": "string",
    "task_info": { "external_task_id": "string" },
    "created_at": 1722769557708,
    "updated_at": 1722769557708
  }
}
```

### Query Task (Single)

**GET** `/v1/videos/video-extend/{task_id}`

#### Path Parameters

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `task_id` | `string` | Optional | Task ID. Choose either task_id or external_task_id. |
| `external_task_id` | `string` | Optional | Custom task ID. |

#### Response

Includes `task_info.parent_video` with the original video's `id`, `url`, and `duration`, plus `task_result.videos[]` with generated extended video details.

### Query Task (List)

**GET** `/v1/videos/video-extend`

#### Query Parameters

| Name | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| `pageNum` | `int` | Optional | `1` | Page number. Range: [1, 1000] |
| `pageSize` | `int` | Optional | `30` | Items per page. Range: [1, 500] |

---

## Authentication

JWT Bearer token using HS256 with access key and secret key. Token validity: 30 minutes.

## Callback Protocol

POST notifications on task status changes when callback_url is configured. Generated assets cleared after 30 days.
