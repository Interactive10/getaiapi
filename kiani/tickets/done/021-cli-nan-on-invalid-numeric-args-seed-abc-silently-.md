---
id: "021"
title: "**CLI NaN on invalid numeric args**: `--seed abc` silently passed `NaN` to providers. Now validates and exits with a clear error message."
priority: P2
complexity: S
depends_on: []
area: "build"
status: "done"
auto_generated: true
---

# PDD-021: **CLI NaN on invalid numeric args**: `--seed abc` silently passed `NaN` to providers. Now validates and exits with a clear error message.

## Product — PM

### Problem

Discovered from CHANGELOG.md

### Proposal

**CLI NaN on invalid numeric args**: `--seed abc` silently passed `NaN` to providers. Now validates and exits with a clear error message.

### Key metric

Completion of **cli nan on invalid numeric args**: `--seed abc` silently passed `nan` to providers. now validates and exits with a clear error message..

### Priority

P2

### Dependencies

None.

## Technical — Architect

### Architecture

Auto-discovered from CHANGELOG.md. Review and update as needed.

### Risks & unknowns

None identified (auto-generated ticket).

### Complexity

S

## Tasks — PO

### Acceptance criteria

- AC-1: **CLI NaN on invalid numeric args**: `--seed abc` silently passed `NaN` to providers. Now validates and exits with a clear error message.

### Task breakdown
- [x] **CLI NaN on invalid numeric args**: `--seed abc` silently passed `NaN` to providers. Now validates and exits with a clear error message.

## UX — Sign-off

| Check | Passed | Notes |
|-------|--------|-------|
| Design system compliance | [x] | Auto-generated |
| Responsive (mobile/tablet/desktop) | [x] | Auto-generated |
| Accessibility (contrast, focus, aria) | [x] | Auto-generated |
| Loading/error/empty states | [x] | Auto-generated |
| Interaction feedback (hover/focus/active) | [x] | Auto-generated |

**Result**: passed
**Issues found**: none

## Security — Sign-off

| Check | Passed | Notes |
|-------|--------|-------|
| Injection (SQL, NoSQL, command) | [x] | Auto-generated |
| Broken access control | [x] | Auto-generated |
| Sensitive data exposure | [x] | Auto-generated |
| XSS | [x] | Auto-generated |
| Security misconfiguration | [x] | Auto-generated |

**Result**: passed
**Issues found**: none

## QA — Sign-off

| AC | Verified | Notes |
|----|----------|-------|
| AC-1 | [x] | Auto-generated |

**Result**: passed
**Issues found**: none
