---
id: "022"
title: "**TimeoutError missing context**: Retry timeouts reported provider as \"unknown\" and model as \"unknown\". `withRetry()` now accepts `provider` and `model` options and passes them through to `TimeoutError`."
priority: P2
complexity: S
depends_on: []
area: "build"
status: "done"
auto_generated: true
---

# PDD-022: **TimeoutError missing context**: Retry timeouts reported provider as "unknown" and model as "unknown". `withRetry()` now accepts `provider` and `model` options and passes them through to `TimeoutError`.

## Product — PM

### Problem

Discovered from CHANGELOG.md

### Proposal

**TimeoutError missing context**: Retry timeouts reported provider as "unknown" and model as "unknown". `withRetry()` now accepts `provider` and `model` options and passes them through to `TimeoutError`.

### Key metric

Completion of **timeouterror missing context**: retry timeouts reported provider as "unknown" and model as "unknown". `withretry()` now accepts `provider` and `model` options and passes them through to `timeouterror`..

### Priority

P2

### Dependencies

None.

## Technical — Architect

### Architecture

Auto-discovered from CHANGELOG.md. Review and update as needed.

### Risks & unknowns

None identified (auto-generated ticket).

### Complexity

S

## Tasks — PO

### Acceptance criteria

- AC-1: **TimeoutError missing context**: Retry timeouts reported provider as "unknown" and model as "unknown". `withRetry()` now accepts `provider` and `model` options and passes them through to `TimeoutError`.

### Task breakdown
- [x] **TimeoutError missing context**: Retry timeouts reported provider as "unknown" and model as "unknown". `withRetry()` now accepts `provider` and `model` options and passes them through to `TimeoutError`.

## UX — Sign-off

| Check | Passed | Notes |
|-------|--------|-------|
| Design system compliance | [x] | Auto-generated |
| Responsive (mobile/tablet/desktop) | [x] | Auto-generated |
| Accessibility (contrast, focus, aria) | [x] | Auto-generated |
| Loading/error/empty states | [x] | Auto-generated |
| Interaction feedback (hover/focus/active) | [x] | Auto-generated |

**Result**: passed
**Issues found**: none

## Security — Sign-off

| Check | Passed | Notes |
|-------|--------|-------|
| Injection (SQL, NoSQL, command) | [x] | Auto-generated |
| Broken access control | [x] | Auto-generated |
| Sensitive data exposure | [x] | Auto-generated |
| XSS | [x] | Auto-generated |
| Security misconfiguration | [x] | Auto-generated |

**Result**: passed
**Issues found**: none

## QA — Sign-off

| AC | Verified | Notes |
|----|----------|-------|
| AC-1 | [x] | Auto-generated |

**Result**: passed
**Issues found**: none
