---
id: "045"
title: "17 FLUX models: `image-edit`/`training` → `text-to-image` (no image input required); `text-to-image` → `image-edit`/`image-to-image` (image input required)"
priority: P2
complexity: S
depends_on: []
area: "build"
status: "done"
auto_generated: true
---

# PDD-045: 17 FLUX models: `image-edit`/`training` → `text-to-image` (no image input required); `text-to-image` → `image-edit`/`image-to-image` (image input required)

## Product — PM

### Problem

Discovered from CHANGELOG.md

### Proposal

17 FLUX models: `image-edit`/`training` → `text-to-image` (no image input required); `text-to-image` → `image-edit`/`image-to-image` (image input required)

### Key metric

Completion of 17 flux models: `image-edit`/`training` → `text-to-image` (no image input required); `text-to-image` → `image-edit`/`image-to-image` (image input required).

### Priority

P2

### Dependencies

None.

## Technical — Architect

### Architecture

Auto-discovered from CHANGELOG.md. Review and update as needed.

### Risks & unknowns

None identified (auto-generated ticket).

### Complexity

S

## Tasks — PO

### Acceptance criteria

- AC-1: 17 FLUX models: `image-edit`/`training` → `text-to-image` (no image input required); `text-to-image` → `image-edit`/`image-to-image` (image input required)

### Task breakdown
- [x] 17 FLUX models: `image-edit`/`training` → `text-to-image` (no image input required); `text-to-image` → `image-edit`/`image-to-image` (image input required)

## UX — Sign-off

| Check | Passed | Notes |
|-------|--------|-------|
| Design system compliance | [x] | Auto-generated |
| Responsive (mobile/tablet/desktop) | [x] | Auto-generated |
| Accessibility (contrast, focus, aria) | [x] | Auto-generated |
| Loading/error/empty states | [x] | Auto-generated |
| Interaction feedback (hover/focus/active) | [x] | Auto-generated |

**Result**: passed
**Issues found**: none

## Security — Sign-off

| Check | Passed | Notes |
|-------|--------|-------|
| Injection (SQL, NoSQL, command) | [x] | Auto-generated |
| Broken access control | [x] | Auto-generated |
| Sensitive data exposure | [x] | Auto-generated |
| XSS | [x] | Auto-generated |
| Security misconfiguration | [x] | Auto-generated |

**Result**: passed
**Issues found**: none

## QA — Sign-off

| AC | Verified | Notes |
|----|----------|-------|
| AC-1 | [x] | Auto-generated |

**Result**: passed
**Issues found**: none
