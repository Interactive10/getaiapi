---
id: "064"
title: "**16 cjwbw models recategorized**: 5 to `upscale-image` (real-esrgan, supir, supir-v0f, supir-v0q, vqfr), 2 to `audio-to-video` (aniportrait-audio2vid, sadtalker), 2 to `text-to-audio` (seamless_communication, voicecraft), 2 to `image-to-image` (bigcolor, face-align-cog), 1 to `video-to-video` (controlvideo), 1 to `doc-to-text` (docentr), 1 to `image-to-text` (internlm-xcomposer), 1 to `remove-background` (rmgb), 1 to `text-to-3d` (shap-e). Previously miscategorized as `text-to-image`. Fixed modality inputs, outputs, and output_map for all 16."
priority: P2
complexity: S
depends_on: []
area: "build"
status: "done"
auto_generated: true
---

# PDD-064: **16 cjwbw models recategorized**: 5 to `upscale-image` (real-esrgan, supir, supir-v0f, supir-v0q, vqfr), 2 to `audio-to-video` (aniportrait-audio2vid, sadtalker), 2 to `text-to-audio` (seamless_communication, voicecraft), 2 to `image-to-image` (bigcolor, face-align-cog), 1 to `video-to-video` (controlvideo), 1 to `doc-to-text` (docentr), 1 to `image-to-text` (internlm-xcomposer), 1 to `remove-background` (rmgb), 1 to `text-to-3d` (shap-e). Previously miscategorized as `text-to-image`. Fixed modality inputs, outputs, and output_map for all 16.

## Product — PM

### Problem

Discovered from CHANGELOG.md

### Proposal

**16 cjwbw models recategorized**: 5 to `upscale-image` (real-esrgan, supir, supir-v0f, supir-v0q, vqfr), 2 to `audio-to-video` (aniportrait-audio2vid, sadtalker), 2 to `text-to-audio` (seamless_communication, voicecraft), 2 to `image-to-image` (bigcolor, face-align-cog), 1 to `video-to-video` (controlvideo), 1 to `doc-to-text` (docentr), 1 to `image-to-text` (internlm-xcomposer), 1 to `remove-background` (rmgb), 1 to `text-to-3d` (shap-e). Previously miscategorized as `text-to-image`. Fixed modality inputs, outputs, and output_map for all 16.

### Key metric

Completion of **16 cjwbw models recategorized**: 5 to `upscale-image` (real-esrgan, supir, supir-v0f, supir-v0q, vqfr), 2 to `audio-to-video` (aniportrait-audio2vid, sadtalker), 2 to `text-to-audio` (seamless_communication, voicecraft), 2 to `image-to-image` (bigcolor, face-align-cog), 1 to `video-to-video` (controlvideo), 1 to `doc-to-text` (docentr), 1 to `image-to-text` (internlm-xcomposer), 1 to `remove-background` (rmgb), 1 to `text-to-3d` (shap-e). previously miscategorized as `text-to-image`. fixed modality inputs, outputs, and output_map for all 16..

### Priority

P2

### Dependencies

None.

## Technical — Architect

### Architecture

Auto-discovered from CHANGELOG.md. Review and update as needed.

### Risks & unknowns

None identified (auto-generated ticket).

### Complexity

S

## Tasks — PO

### Acceptance criteria

- AC-1: **16 cjwbw models recategorized**: 5 to `upscale-image` (real-esrgan, supir, supir-v0f, supir-v0q, vqfr), 2 to `audio-to-video` (aniportrait-audio2vid, sadtalker), 2 to `text-to-audio` (seamless_communication, voicecraft), 2 to `image-to-image` (bigcolor, face-align-cog), 1 to `video-to-video` (controlvideo), 1 to `doc-to-text` (docentr), 1 to `image-to-text` (internlm-xcomposer), 1 to `remove-background` (rmgb), 1 to `text-to-3d` (shap-e). Previously miscategorized as `text-to-image`. Fixed modality inputs, outputs, and output_map for all 16.

### Task breakdown
- [x] **16 cjwbw models recategorized**: 5 to `upscale-image` (real-esrgan, supir, supir-v0f, supir-v0q, vqfr), 2 to `audio-to-video` (aniportrait-audio2vid, sadtalker), 2 to `text-to-audio` (seamless_communication, voicecraft), 2 to `image-to-image` (bigcolor, face-align-cog), 1 to `video-to-video` (controlvideo), 1 to `doc-to-text` (docentr), 1 to `image-to-text` (internlm-xcomposer), 1 to `remove-background` (rmgb), 1 to `text-to-3d` (shap-e). Previously miscategorized as `text-to-image`. Fixed modality inputs, outputs, and output_map for all 16.

## UX — Sign-off

| Check | Passed | Notes |
|-------|--------|-------|
| Design system compliance | [x] | Auto-generated |
| Responsive (mobile/tablet/desktop) | [x] | Auto-generated |
| Accessibility (contrast, focus, aria) | [x] | Auto-generated |
| Loading/error/empty states | [x] | Auto-generated |
| Interaction feedback (hover/focus/active) | [x] | Auto-generated |

**Result**: passed
**Issues found**: none

## Security — Sign-off

| Check | Passed | Notes |
|-------|--------|-------|
| Injection (SQL, NoSQL, command) | [x] | Auto-generated |
| Broken access control | [x] | Auto-generated |
| Sensitive data exposure | [x] | Auto-generated |
| XSS | [x] | Auto-generated |
| Security misconfiguration | [x] | Auto-generated |

**Result**: passed
**Issues found**: none

## QA — Sign-off

| AC | Verified | Notes |
|----|----------|-------|
| AC-1 | [x] | Auto-generated |

**Result**: passed
**Issues found**: none
