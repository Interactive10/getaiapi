---
id: "084"
title: "**codeplugtech-face-swap**: Recategorized from `text-to-image` to `image-to-image` — model takes two images (no prompt), but `text-to-image` validation rejected requests without a prompt."
priority: P2
complexity: S
depends_on: []
area: "build"
status: "done"
auto_generated: true
---

# PDD-084: **codeplugtech-face-swap**: Recategorized from `text-to-image` to `image-to-image` — model takes two images (no prompt), but `text-to-image` validation rejected requests without a prompt.

## Product — PM

### Problem

Discovered from CHANGELOG.md

### Proposal

**codeplugtech-face-swap**: Recategorized from `text-to-image` to `image-to-image` — model takes two images (no prompt), but `text-to-image` validation rejected requests without a prompt.

### Key metric

Completion of **codeplugtech-face-swap**: recategorized from `text-to-image` to `image-to-image` — model takes two images (no prompt), but `text-to-image` validation rejected requests without a prompt..

### Priority

P2

### Dependencies

None.

## Technical — Architect

### Architecture

Auto-discovered from CHANGELOG.md. Review and update as needed.

### Risks & unknowns

None identified (auto-generated ticket).

### Complexity

S

## Tasks — PO

### Acceptance criteria

- AC-1: **codeplugtech-face-swap**: Recategorized from `text-to-image` to `image-to-image` — model takes two images (no prompt), but `text-to-image` validation rejected requests without a prompt.

### Task breakdown
- [x] **codeplugtech-face-swap**: Recategorized from `text-to-image` to `image-to-image` — model takes two images (no prompt), but `text-to-image` validation rejected requests without a prompt.

## UX — Sign-off

| Check | Passed | Notes |
|-------|--------|-------|
| Design system compliance | [x] | Auto-generated |
| Responsive (mobile/tablet/desktop) | [x] | Auto-generated |
| Accessibility (contrast, focus, aria) | [x] | Auto-generated |
| Loading/error/empty states | [x] | Auto-generated |
| Interaction feedback (hover/focus/active) | [x] | Auto-generated |

**Result**: passed
**Issues found**: none

## Security — Sign-off

| Check | Passed | Notes |
|-------|--------|-------|
| Injection (SQL, NoSQL, command) | [x] | Auto-generated |
| Broken access control | [x] | Auto-generated |
| Sensitive data exposure | [x] | Auto-generated |
| XSS | [x] | Auto-generated |
| Security misconfiguration | [x] | Auto-generated |

**Result**: passed
**Issues found**: none

## QA — Sign-off

| AC | Verified | Notes |
|----|----------|-------|
| AC-1 | [x] | Auto-generated |

**Result**: passed
**Issues found**: none
