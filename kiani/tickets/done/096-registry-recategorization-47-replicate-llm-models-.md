---
id: "096"
title: "**Registry recategorization**: 47 Replicate LLM models (Claude, GPT, Llama, DeepSeek, Gemma, Mistral, Qwen, LLaVA, etc.) miscategorized as `text-to-image` → `text-generation`. 4 guard/moderation models (llama-guard, qwen-3-guard) → `moderation`. 1 video LLM (videollama3) → `text-generation`."
priority: P2
complexity: S
depends_on: []
area: "build"
status: "done"
auto_generated: true
---

# PDD-096: **Registry recategorization**: 47 Replicate LLM models (Claude, GPT, Llama, DeepSeek, Gemma, Mistral, Qwen, LLaVA, etc.) miscategorized as `text-to-image` → `text-generation`. 4 guard/moderation models (llama-guard, qwen-3-guard) → `moderation`. 1 video LLM (videollama3) → `text-generation`.

## Product — PM

### Problem

Discovered from CHANGELOG.md

### Proposal

**Registry recategorization**: 47 Replicate LLM models (Claude, GPT, Llama, DeepSeek, Gemma, Mistral, Qwen, LLaVA, etc.) miscategorized as `text-to-image` → `text-generation`. 4 guard/moderation models (llama-guard, qwen-3-guard) → `moderation`. 1 video LLM (videollama3) → `text-generation`.

### Key metric

Completion of **registry recategorization**: 47 replicate llm models (claude, gpt, llama, deepseek, gemma, mistral, qwen, llava, etc.) miscategorized as `text-to-image` → `text-generation`. 4 guard/moderation models (llama-guard, qwen-3-guard) → `moderation`. 1 video llm (videollama3) → `text-generation`..

### Priority

P2

### Dependencies

None.

## Technical — Architect

### Architecture

Auto-discovered from CHANGELOG.md. Review and update as needed.

### Risks & unknowns

None identified (auto-generated ticket).

### Complexity

S

## Tasks — PO

### Acceptance criteria

- AC-1: **Registry recategorization**: 47 Replicate LLM models (Claude, GPT, Llama, DeepSeek, Gemma, Mistral, Qwen, LLaVA, etc.) miscategorized as `text-to-image` → `text-generation`. 4 guard/moderation models (llama-guard, qwen-3-guard) → `moderation`. 1 video LLM (videollama3) → `text-generation`.

### Task breakdown
- [x] **Registry recategorization**: 47 Replicate LLM models (Claude, GPT, Llama, DeepSeek, Gemma, Mistral, Qwen, LLaVA, etc.) miscategorized as `text-to-image` → `text-generation`. 4 guard/moderation models (llama-guard, qwen-3-guard) → `moderation`. 1 video LLM (videollama3) → `text-generation`.

## UX — Sign-off

| Check | Passed | Notes |
|-------|--------|-------|
| Design system compliance | [x] | Auto-generated |
| Responsive (mobile/tablet/desktop) | [x] | Auto-generated |
| Accessibility (contrast, focus, aria) | [x] | Auto-generated |
| Loading/error/empty states | [x] | Auto-generated |
| Interaction feedback (hover/focus/active) | [x] | Auto-generated |

**Result**: passed
**Issues found**: none

## Security — Sign-off

| Check | Passed | Notes |
|-------|--------|-------|
| Injection (SQL, NoSQL, command) | [x] | Auto-generated |
| Broken access control | [x] | Auto-generated |
| Sensitive data exposure | [x] | Auto-generated |
| XSS | [x] | Auto-generated |
| Security misconfiguration | [x] | Auto-generated |

**Result**: passed
**Issues found**: none

## QA — Sign-off

| AC | Verified | Notes |
|----|----------|-------|
| AC-1 | [x] | Auto-generated |

**Result**: passed
**Issues found**: none
